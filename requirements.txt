# Pip requirements for platforms that don't use uv/pyproject (e.g. Replit).
# Relaxed lower bounds so a public package index (PyPI) can resolve them.
numpy>=1.26
pandas>=2.1
plotly>=5.18
scipy>=1.11
streamlit>=1.30
yfinance>=0.2.40
